collectors:

- type: js-npm
  path: /
  actors:
  - type: js-npm
    versions: "Y.Y.Y"
    settings:
      batch_mode: true
      related_pr_behavior: close
      github_assignees:
      - nathunsmitty
      # github_labels:
      # - dependency
